import React from 'react';

const HeroSection = () => (
  <section className="hero">
    <h2>Welcome to Your Material Home</h2>
    <p>Discover a modern, clean, material-like layout built with React.</p>
  </section>
);

export default HeroSection;
